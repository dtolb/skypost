// Compose — placeholder. The composer screen + facet parsing helper land
// here in the Compose dispatch (§11 step 4).

import Foundation

public enum ComposeFeature {
    public static let moduleName = "Compose"
}
