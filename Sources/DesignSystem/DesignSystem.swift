// DesignSystem — placeholder. Tokens, colors, internal Buttons/Cards land
// here when we have shared styling worth extracting. Per §12 open question:
// don't pre-build a kit — grow this module only when the same styling
// gets copy-pasted 3+ times.

import Foundation

public enum DesignSystem {
    public static let moduleName = "DesignSystem"
}
